/*global define*/

define([], function () {
  'use strict';

  var Common = {
    Repositories: {
      usersSequence: 'users-sequence',
      currentUserId: 'current-user-id',
      users: 'users-'
    }
  }

  return Common;
});
